# Deixe em branco/comentado para usar automaticamente o mesmo IP/host que
# você digitou no navegador (necessário para acessar do celular pela rede local).
# Só defina isto se a API estiver em outro endereço (ex.: produção).
# VITE_API_URL=http://localhost:3000
